services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: stitchr
      POSTGRES_PASSWORD: stitchr
      POSTGRES_DB: stitchr
    ports: ['5432:5432']
    volumes: ['registry:/var/lib/postgresql/data']
volumes:
  registry:
